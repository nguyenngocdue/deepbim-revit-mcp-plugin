# Services

Reusable service layer for the tools project.

Service categories:
- `Core`: cross-cutting infrastructure used by many tools.
- `Features`: domain-specific services grouped by capability.
