# ViewModels Base

Base ViewModel infrastructure.

`ToolViewModelBase` provides:
- shared state (`ResultText`, `ErrorText`, execution flags)
- common run flow
- property change notification for binding-ready usage
