# Commands Base

Base command infrastructure for all tool commands.

`BaseToolCommand<TViewModel>` provides a shared execution template:
- build services and view adapters
- run the ViewModel
- handle success and error output consistently
