<Window x:Class="revit_mcp_plugin.UI.SettingsWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:revit_mcp_plugin.UI"
             mc:Ignorable="d" 
             Title="DeepBim-MCP Settings" Height="500" Width="850" Background="White">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/RevitMCPPlugin;component/Resources/Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Navigation -->
        <Border Grid.Column="0" Background="{StaticResource GrayLight}" BorderBrush="{StaticResource Border}" BorderThickness="0,0,1,0">
            <ListBox x:Name="NavListBox" BorderThickness="0" Background="Transparent" 
                     SelectionChanged="NavListBox_SelectionChanged">
                <ListBoxItem x:Name="CommandSetItem" IsSelected="True" Padding="10,8">
                    <TextBlock Text="Command Sets" Style="{StaticResource TextHeader}" FontSize="14"/>
                </ListBoxItem>
            </ListBox>
        </Border>

        <!-- Right Content Area -->
        <Grid Grid.Column="1" Margin="15">
            <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Window>
