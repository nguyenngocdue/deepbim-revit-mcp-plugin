<UserControl x:Class="revit_mcp_plugin.UI.ExportSheetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F8F9FA">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select sheet properties to export"
                   FontSize="16" FontWeight="SemiBold" Foreground="#212121" Margin="0,0,0,12"/>

        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="10,8" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="SelectAllButton" Content="Select all" Width="90" Height="28" Margin="0,0,8,0"
                        Click="SelectAllButton_Click" Background="#59DCCB" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                <Button x:Name="DeselectAllButton" Content="Deselect all" Width="90" Height="28"
                        Click="DeselectAllButton_Click" Background="#F4F4F5" Foreground="#71717A" BorderThickness="1" BorderBrush="#D4D4D4" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="PropertiesItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                      Margin="4,2" Foreground="#212121" FontSize="13"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="3" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="10,8" Margin="0,12,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathTextBox" Grid.Column="0" Height="32" VerticalContentAlignment="Center"
                         Padding="8,4" Margin="0,0,8,0" IsReadOnly="True" Background="#F8F9FA"/>
                <Button Grid.Column="1" Content="Browse…" Width="80" Height="32"
                        Click="BrowseButton_Click" Background="#59DCCB" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="ExportButton" Content="Export to Excel" Width="120" Height="36"
                    Click="ExportButton_Click" Background="#59DCCB" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"/>
            <Button Content="Cancel" Width="80" Height="36" Margin="8,0,0,0"
                    Click="CancelButton_Click" Background="#F4F4F5" Foreground="#71717A" BorderThickness="1" BorderBrush="#D4D4D4" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</UserControl>
