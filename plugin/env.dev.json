{
  "mode": "dev",
  "_comment": "Mode = dev when building with Configuration = Debug. Project copies this to deepbim-mcp.env.json in output folder (AddIn <version> Debug\\revit_mcp_plugin). Version comes from RevitVersions.json."
}
