{
  "mode": "dev",
  "_comment": "Mode = dev when building with Configuration = Debug. Project copies this to deepbim-mcp.env.json in output folder (AddIn 2025 Debug\\revit_mcp_plugin). No manual setup needed."
}
